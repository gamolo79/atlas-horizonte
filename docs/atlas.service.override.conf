[Service]
EnvironmentFile=/srv/atlas/.env

# Reset del ExecStart original y nuevo ExecStart completo
ExecStart=
ExecStart=/srv/atlas/venv/bin/gunicorn atlas_core.wsgi:application --bind 127.0.0.1:8000 --workers 3 --timeout 180
